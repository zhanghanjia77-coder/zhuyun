-- ============================================================
-- 清空所有「注册用户」相关数据（public.profiles + auth.users）
--
-- 用途：个人/测试环境想从零重新注册时，在控制台一次性清空账号。
-- 执行位置：Supabase 控制台 → SQL → New query → 粘贴本文件全文 → Run
--
-- 警告：
--   · 不可恢复，请先确认当前选中的项目正确
--   · 勿对已有真实用户的生产库执行
--   · 执行后所有已注册邮箱均可重新走注册流程
-- ============================================================

BEGIN;

-- 1. 业务侧用户资料（外键依赖 auth.users，须先于用户删除）
DELETE FROM public.profiles;

-- 2. 认证用户（identities、sessions 等由 Supabase 内部外键级联处理）
DELETE FROM auth.users;

COMMIT;

-- ------------------------------------------------------------
-- 可选：若曾在存储桶 zhuyun-assets 中上传过与用户相关的文件，
-- 可在确认无其他重要文件后，取消下面三行的注释再单独执行一段。
-- （一般仅清账号时可以不执行。）
--
-- DELETE FROM storage.objects WHERE bucket_id = 'zhuyun-assets';
-- ------------------------------------------------------------
