# Supabase（在控制台 Project Settings → API 获取）
# 注册/重置密码邮件中的链接：在 Authentication → URL Configuration 的 Redirect URLs 中加入
#   http://localhost:5173/auth/callback（生产环境再加你的线上域名 + /auth/callback）
# 手机访问电脑上的开发站：用电脑局域网 IP（如 http://192.168.1.5:5173），并在 Redirect URLs 中加入
#   http://该IP:5173/auth/callback（与邮件里跳转的主机名一致）；本项目 Vite 已开启 server.host 便于局域网调试。
# 应用内：/login、/register 直达「我的」对应选项卡；登录成功可带 ?next=/library/xxx 跳回站内路径（与邮件链接无关）
# 邮件里的验证/重置链接仅使用「无参数」回调：须与白名单中的 http(s)://你的域名/auth/callback 完全一致
#
# —— 电脑注册也收不到验证邮件时（多为控制台配置，不是前端代码）——
# 1) Authentication → Providers → Email：确认已开启；并查看是否要求「邮箱验证」。
# 2) Authentication → Emails：务必配置「自定义 SMTP」（企业邮箱 / Resend / 阿里云邮件等）；未配置时内置发信极易失败或进垃圾箱。
# 3) Logs：查看 Auth 相关报错（SMTP、限流、域名未验证等）。
# 4) 确认 .env 里 VITE_SUPABASE_URL 与当前控制台项目一致，避免连错项目。
# 5) 开发阶段可在 Authentication → Providers → Email 暂时关闭「Confirm email」以跳过验证（仅测试用，勿用于生产）。
#
# —— 「同步到账号」保存昵称失败时 ——
# 在 SQL Editor 依次执行 03_profiles_authenticated_grants.sql 与 04_profile_save_rpc.sql。
# 若提示 PGRST204 / 找不到 profiles.email 列：先执行 02_auth_profile_sync.sql 再重试（或等待 API schema 刷新）。
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
